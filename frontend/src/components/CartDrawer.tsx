'use client';
import { motion, AnimatePresence } from 'framer-motion';
import { X } from 'lucide-react';
import type { Product } from '@/lib/types';
import { money } from '@/lib/utils';
export function CartDrawer({ open, items, onClose }: { open: boolean; items: Product[]; onClose: ()=>void }) { const total=items.reduce((s,p)=>s+(p.discount_price||p.price),0); return <AnimatePresence>{open && <motion.div className="fixed inset-0 z-50 bg-slate-950/60 backdrop-blur" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div initial={{x:420}} animate={{x:0}} exit={{x:420}} className="ml-auto h-full w-full max-w-md border-l border-white/10 bg-slate-950 p-6 shadow-panel"><div className="flex items-center justify-between"><h2 className="text-2xl font-semibold text-white">Cart</h2><button onClick={onClose} className="rounded-xl bg-white/10 p-2"><X/></button></div><div className="mt-6 space-y-3">{items.map((p,i)=><div key={`${p.id}-${i}`} className="rounded-2xl border border-white/10 bg-white/[.04] p-4"><div className="font-medium text-white">{p.title}</div><div className="text-sm text-slate-400">{money(p.discount_price||p.price)}</div></div>)}</div><div className="mt-8 rounded-3xl border border-cyan-300/20 bg-cyan-300/10 p-5"><div className="flex justify-between text-lg text-white"><span>Total</span><span>{money(total)}</span></div><button className="mt-5 w-full rounded-2xl bg-cyan-300 px-4 py-3 font-semibold text-slate-950">Checkout with mock payment</button></div></motion.div></motion.div>}</AnimatePresence> }
